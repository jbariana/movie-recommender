# Movie Recommender - sample env
# Postgres connection for persistence
DATABASE_URL=postgresql://movie:moviepwd@localhost:5432/movies

# Optional: fallback SQLite path (only used if DATABASE_URL is unset)
# DB_PATH=database/movies.db

# Dev conveniences
# FLASK_APP=app.py
# FLASK_ENV=development
# PYTHONPATH=.
