# --- Python caches
__pycache__/
*.py[cod]
*.pyo

# --- Virtual envs
.venv/
venv/
.env/           # virtualenv-style dirs sometimes named .env

# --- Local env files
.env
.env.*
.envrc
.python-version   # pyenv

# --- Local DB artifacts
*.db
*.sqlite
*.sqlite3
database/*.db

# --- Flask instance (runtime secrets/uploads)
instance/

# --- OS / editors
.DS_Store
.idea/
.vscode/

# --- Testing / coverage / logs
.pytest_cache/
.coverage
.coverage.*
htmlcov/
*.log

# --- Packaging / builds
build/
dist/
*.egg-info/

# --- Jupyter
.ipynb_checkpoints/

# --- Data
# Keep your own small CSVs, but ignore bulky archives and the MovieLens dump
data/*.zip
data/ml-latest-small/
# (If you want the folder in Git but empty, add a data/.gitkeep)

# --- App runtime profile (local-only)
user_profile/user_profile.json
